//this is going to check whether the login is right
//and add/remove certain content, 
// like the log in button if the user is already logged in.
//or an admin panel.

window.addEventListener("DOMContentLoaded", () => {
    const logInButton = document.getElementById('login')
    const logOutButton = document.getElementById('logout')


});